# 08 · 路线图

> 状态：**基线文档** · 关联：[05-实现方案](05-实现方案.md) · [07-发布与分享](07-发布与分享.md)
> 状态标记：☑️ 已完成 · 🔄 进行中 · ⬜ 未开始（实现细节与历史见 CHANGELOG.md 与 git 历史）

## 1. 里程碑

| 里程碑 | 目标 | 交付物 | 验收（见 06-§5） |
| --- | --- | --- | --- |
| **M1** 骨架与预览 | 可安装插件壳 + config.toml 解析 + 预览工具 | package.json / cordis.patch.yml / index.mjs / lib（config、yaml、report、ledger、paths）/ `migrate_codex_preview` / 单测 | ✅ 本机 `~/.codex` 预览输出完整清单；`npm test` 绿 |
| **M2** 核心迁移 | MCP 镜像 + 技能转换 | lib/mcp.mjs、lib/skills.mjs、`migrate_codex_mcp`、`migrate_codex_skills` + 夹具单测 | ✅ dry-run/apply/幂等全符合映射规范；fixture 快照锁定；真实环境 dry-run 验证 27 技能 |
| **M3** 全资产覆盖 | 指令/记忆/配置建议 + 台账 + doctor | lib/instructions.mjs、lib/memory.mjs、lib/settings.mjs、lib/doctor.mjs、`migrate_codex_instructions/memory/config`、`codex2dsh_doctor/ledger` | ✅ 真实环境 dry-run 验证：指令 3 项、记忆 3 文件 + sqlite 探测、配置建议完整、doctor 全类别状态 |
| **M4** 会话与入口 | 会话委托 + CLI + 面板（可选） | lib/delegate.mjs、bin/codex2dsh.mjs、（lib/panel.mjs、lib/command.mjs） | ✅ 会话委托（统计+安装指引+透传委托）、CLI 全子命令、L3 冒烟 16 项通过；面板/斜杠命令为可选项（REQ-13/14） |
| **M5** 发布 | 测试验收 + 发布分享 | CI 绿、npm 包、awesome 收录 PR、发布检查清单 | ⬜ 07-§7 清单全勾 |

## 2. 需求清单（REQ）

| ID | 优先级 | 需求 | 里程碑 | 状态 |
| --- | --- | --- | --- | --- |
| REQ-01 | P0 | config.toml 轻量解析（mcp_servers/model_providers/projects/features/memories/windows），损坏降级 | M1 | ☑️ |
| REQ-02 | P0 | 统一预览（dry-run）语义与统一报告结构 | M1 | ☑️ |
| REQ-03 | P0 | 密钥脱敏（password/token/key/secret/bearer）与"环境变量化"建议 | M1 | ☑️ |
| REQ-04 | P0 | 迁移台账（$DSH_HOME/codex2dsh/ledger.json）+ 幂等判定 | M1 | ☑️ |
| REQ-05 | P0 | MCP 镜像生成（含运行时服务器排除、startup_timeout_sec、env 透传） | M2 | ☑️ |
| REQ-06 | P0 | 技能转换落盘（frontmatter kind:dsh/source、scripts 复制、冲突 -codex 后缀、内容相同跳过） | M2 | ☑️ |
| REQ-07 | P1 | 全局指令迁移（AGENTS.md/instructions.md → $DSH_AGENTS_HOME/instructions/，原文完整） | M3 | ☑️ |
| REQ-08 | P1 | 记忆迁移（memories/ 可读资产；sqlite 只读探测降级报告） | M3 | ☑️ |
| REQ-09 | P1 | 模型/Provider/权限/项目信任 → 只读建议（settings-suggest.yml，绝不自动改 settings.yaml） | M3 | ☑️ |
| REQ-10 | P1 | codex2dsh_doctor 体检（含密钥残留扫描） | M3 | ☑️ |
| REQ-11 | P1 | 会话委托 import_chat（探测→委托→台账；未装→安装指引） | M4 | ☑️ |
| REQ-12 | P2 | CLI bin（preview/mcp/skills/instructions/memory/config/sessions/doctor/ledger） | M4 | ☑️ |
| REQ-13 | P2 | 面板路由 /codex2dsh/*（webServer 延后挂载） | M4 | ☑️（lib/panel.mjs：status/preview/migrate + client/index.js 设置页 Tab） |
| REQ-14 | P2 | 斜杠命令 /codex2dsh | M4 | ⬜（可选项，未实现） |
| REQ-15 | P1 | 单元测试 ≥ 75% 行覆盖 + 夹具敏感词 CI 检查 | M1–M3 | ☑️（93.10% 行覆盖） |
| REQ-16 | P1 | L3 headless 冒烟（临时环境变量指向夹具，绝不碰真实目录） | M4 | ☑️（.github/scripts/smoke.mjs 16 项断言，CI 已接入） |
| REQ-17 | P0 | 发布门禁：无生命周期脚本、包体含 cordis.patch.yml、prepublishOnly 测试 | M5 | ⬜ |
| REQ-18 | P2 | awesome-dsh-plugin.com 收录 PR + 聚合渠道 | M5 | ⬜ |
| REQ-19 | P2 | （可选）dsh-community-market 目录契约来源接入 | M5 | ⬜ |
| REQ-20 | P3 | 双语文档（README.en.md 等） | M5 | ⬜ |

## 3. 演进方向（远期候选）

- Codex → DSH 的**双向同步**（DSH 配置改动写回 Codex 侧），需先解决密钥与格式差异；
- 更多来源（Claude Code 的 skills/CLAUDE.md 已有生态工具 dsh-movein / dsh-chat-import，做统一入口）；
- 迁移前后**配置 diff 可视化**（面板展示）；
- 技能依赖解析（SKILL.md 引用的外部工具链检测与适配报告）。
